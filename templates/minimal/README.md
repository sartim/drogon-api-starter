# @PROJECT_NAME@

Minimal Drogon API generated by `drogon-starter`.

```sh
cmake --preset dev
cmake --build --preset dev
ctest --preset dev
./build/@PROJECT_NAME@
```

Health check:

```sh
curl --fail http://localhost:8000/health
```

Add platform capabilities or a supported service bundle as the application
grows. Do not commit local `.env` files or build output.
