apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base
  - ingress.yaml

patches:
  - path: config.patch.yaml
  - path: deployment.patch.yaml

images:
  - name: ghcr.io/sartim/drogon-api-starter
    newTag: v0.2.0
