apiVersion: v1
kind: Service
metadata:
  name: drogon-api-starter
  labels:
    app.kubernetes.io/name: drogon-api-starter
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: drogon-api-starter
  ports:
    - name: http
      port: 8000
      targetPort: http
      protocol: TCP
